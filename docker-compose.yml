version: '3.8'

services:
  api:
    build: .
    container_name: doctor-appointment-backend
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"